{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow rounded-4">
                <div class="card-body p-4">
                    <h2 class="text-center text-success mb-4">
                        Email de réinitialisation envoyé 📧
                    </h2>

                    <p class="text-muted">
                        Si un compte correspondant à votre adresse email existe, un message a été envoyé contenant un lien pour réinitialiser votre mot de passe.
                    </p>

                    <p class="text-muted">
                        Ce lien expirera dans 
                        <strong>
                            {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
                        </strong>.
                    </p>

                    <p class="text-muted">
                        Si vous ne recevez pas l’email, vérifiez votre dossier spam ou 
                        <a href="{{ path('app_forgot_password_request') }}" class="text-decoration-none">
                            réessayez
                        </a>.
                    </p>

                    <div class="text-center mt-4">
                        <a href="{{ path('app_login') }}" class="btn btn-primary">Retour à la connexion</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
